// Served by the `storyCode` plugin in @kromatv/bundler; no file on disk.

declare module 'virtual:kroma-story-code' {
  import type { StoryCodes } from '@kromatv/workbench';

  export const STORY_CODE: StoryCodes;
}
