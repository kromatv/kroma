// Served by the `kromaIconCatalog` plugin in @kromatv/ui/vite; no file on disk.

declare module 'virtual:kroma-icon-catalog' {
  import type { IconCatalog } from '@kromatv/ui/kit';

  export const ICON_CATALOG: IconCatalog;
}
