//! The schema-bound proof of the storage grant: what a module can and cannot
//! reach in the core database through the scope `kroma-sqlite` installs.

#[cfg(test)]
mod escapes;

#[cfg(test)]
mod tests;
