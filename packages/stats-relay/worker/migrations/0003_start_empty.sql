-- The service starts empty, which is what PRIVACY.md and both legal pages say.
-- What is here is one end-to-end check from August and the fortnight of nightly
-- sweeps that counted it: a published chart opening on a flat line at one, where
-- the one is the maintainer's own dev box, is a worse first impression than an
-- honest zero.
DELETE FROM instances;
DELETE FROM daily;
